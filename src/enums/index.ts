/**
 * Enum for maximum allowed sizes in bytes.
 */
export enum MAX_SIZE {
  /**
   * Maximum image size allowed.
   *
   * @remarks
   * `1 * 1024 * 1024` = **1 MB**
   */
  IMAGE = 2 * 1024 * 1024,
}
